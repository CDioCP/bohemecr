<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bolsos de Macramé Artesanal - Hechos con Amor</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    *{margin:0;padding:0;box-sizing:border-box}
    :root{--primary:#d4a574;--secondary:#2c3e50;--accent:#8b7355;--light:#f8f5f2;--white:#fff;--dark:#1a1a1a}
    body{font-family:'Poppins',sans-serif;color:var(--dark);overflow-x:hidden;background:var(--light)}

    /* Navbar */
    nav{position:fixed;width:100%;padding:1.5rem 5%;background:rgba(255,255,255,.95);
      backdrop-filter:blur(10px);z-index:1000;box-shadow:0 2px 20px rgba(0,0,0,.05);transition:.3s}
    nav.scrolled{padding:1rem 5%;box-shadow:0 2px 30px rgba(0,0,0,.1)}
    .nav-container{max-width:1400px;margin:0 auto;display:flex;justify-content:space-between;align-items:center}
    .logo{font-family:'Playfair Display',serif;font-size:1.8rem;font-weight:700;color:var(--secondary)}
    .nav-links{display:flex;gap:3rem;list-style:none}
    .nav-links a{text-decoration:none;color:var(--secondary);font-weight:500;transition:.3s;position:relative}
    .nav-links a::after{content:'';position:absolute;bottom:-5px;left:0;width:0;height:2px;background:var(--primary);transition:.3s}
    .nav-links a:hover::after{width:100%}
    .nav-links a:hover{color:var(--primary)}

    /* Hero */
    .hero{min-height:100vh;display:flex;align-items:center;padding:8rem 5% 4rem;
      background:linear-gradient(135deg,#f8f5f2 0%,#e8e3dd 100%);position:relative;overflow:hidden}
    .hero-container{max-width:1400px;margin:0 auto;display:grid;grid-template-columns:1fr 1fr;gap:4rem;align-items:center;position:relative;z-index:1}
    .hero-content h1{font-family:'Playfair Display',serif;font-size:4rem;line-height:1.2;color:var(--secondary);margin-bottom:1.5rem}
    .hero-content .subtitle{font-size:1.3rem;color:var(--accent);margin-bottom:2rem;font-weight:300}
    .hero-content p{font-size:1.1rem;line-height:1.8;color:var(--secondary);margin-bottom:3rem;opacity:.9}
    .cta-buttons{display:flex;gap:1.5rem}
    .btn{padding:1rem 2.5rem;border:none;border-radius:50px;font-size:1rem;font-weight:600;cursor:pointer;transition:.3s;text-decoration:none;display:inline-block}
    .btn-primary{background:var(--primary);color:var(--white);box-shadow:0 10px 30px rgba(212,165,116,.3)}
    .btn-primary:hover{transform:translateY(-3px);box-shadow:0 15px 40px rgba(212,165,116,.4)}
    .btn-secondary{background:transparent;color:var(--secondary);border:2px solid var(--secondary)}
    .btn-secondary:hover{background:var(--secondary);color:var(--white);transform:translateY(-3px)}

    /* Images (same size) */
    .hero-images{position:relative}
    .image-grid{display:grid;grid-template-columns: repeat(2, 1fr);gap:2rem;}
    .image-card{position:relative;border-radius:20px;overflow:hidden;box-shadow:0 20px 60px rgba(0,0,0,.15);transition:.5s;background:#fff;}
    .image-card:hover{transform:translateY(-10px) scale(1.02)}
    .image-card img{width:100%;height:600px;object-fit:cover;display:block;}

    /* Contact */
    .contact{padding:6rem 5%;background:linear-gradient(135deg,var(--secondary)0%,var(--accent)100%);color:#fff;text-align:center}
    .contact h2{font-family:'Playfair Display',serif;font-size:3rem;margin-bottom:1.5rem}
    .contact p{font-size:1.2rem;margin-bottom:3rem;opacity:.9}
    .contact-form{max-width:600px;margin:0 auto;display:flex;flex-direction:column;gap:1.2rem}
    .contact-form input,.contact-form textarea{padding:1rem 1.5rem;border:none;border-radius:50px;font-family:'Poppins',sans-serif;font-size:1rem}
    .contact-form textarea{border-radius:20px;resize:vertical;min-height:150px}

    /* Buttons */
    .btn-whatsapp,.btn-email{
      color:white;font-weight:600;border-radius:50px;padding:1rem 2.5rem;
      display:inline-block;text-decoration:none;transition:0.3s;box-shadow:0 5px 15px rgba(0,0,0,0.1);font-size:1.05rem
    }
    .btn-whatsapp{background:#25d366}
    .btn-whatsapp:hover{background:#1ebe5d;transform:translateY(-3px)}
    .btn-email{background:#0072c6;margin-top:1rem}
    .btn-email:hover{background:#005a9e;transform:translateY(-3px)}

    /* Footer */
    footer{background:var(--dark);color:#fff;padding:3rem 5%;text-align:center}
    .social-links{display:flex;justify-content:center;gap:2rem;margin-bottom:2rem}
    .social-links a{color:#fff;font-size:1.5rem;transition:.3s}
    .social-links a:hover{color:var(--primary)}

    /* Responsive */
    @media (max-width:968px){
      .hero-container{grid-template-columns:1fr;text-align:center}
      .image-grid{grid-template-columns:1fr}
      .image-card img{height:460px}
    }
    @media (max-width:640px){
      .hero-content h1{font-size:2.5rem}
      .nav-links{display:none}
      .image-card img{height:380px}
      .cta-buttons{flex-direction:column}
    }
  </style>
</head>
<body>
  <!-- Nav -->
  <nav id="navbar">
    <div class="nav-container">
      <div class="logo">Macramé Artesanal</div>
      <ul class="nav-links">
        <li><a href="#home">Inicio</a></li>
        <li><a href="#collection">Colección</a></li>
        <li><a href="#about">Acerca de</a></li>
        <li><a href="#contact">Contacto</a></li>
      </ul>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero" id="home">
    <div class="hero-container">
      <div class="hero-content">
        <h1>Bolsos de Macramé Hechos a Mano</h1>
        <p class="subtitle">Donde el arte se encuentra con la funcionalidad</p>
        <p>Cada pieza está hecha a mano con materiales de primera calidad, creando accesorios únicos que combinan elegancia atemporal con estilo moderno. Descubre el bolso perfecto que cuenta tu historia.</p>
        <div class="cta-buttons">
          <a href="#collection" class="btn btn-primary">Ver Colección</a>
          <a href="#about" class="btn btn-secondary">Nuestra Historia</a>
        </div>
      </div>

      <div class="hero-images">
        <div class="image-grid">
          <!-- ⚠️ Robust image loader will fill these automatically -->
          <div class="image-card">
            <img id="img-bolso" alt="Bolso de sobre (beige)">
          </div>
          <div class="image-card">
            <img id="img-aura" alt="Bolso Aura Azul">
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section class="contact" id="contact">
    <h2>Hablemos</h2>
    <p>¿Tienes un diseño en mente? Creamos algo hermoso contigo.</p>

    <!-- Form that opens email client (Gmail/Outlook/etc.) prefilled -->
    <form id="contactForm" class="contact-form" action="mailto:marthagch85@gmail.com" method="post">
      <input type="text" id="name" placeholder="Tu nombre" required>
      <input type="email" id="email" placeholder="Tu correo" required>
      <textarea id="message" placeholder="Cuéntanos sobre tu bolso ideal..." required></textarea>
      <button type="submit" class="btn btn-primary">Enviar mensaje</button>
    </form>

    <!-- WhatsApp -->
    <div style="margin-top:2rem;">
      <a href="https://wa.me/50662163804?text=Hola%2C%20estoy%20interesado%20en%20los%20bolsos%20de%20macram%C3%A9%20%E2%9D%A4%EF%B8%8F"
         target="_blank"
         class="btn-whatsapp"
         lang="es">
        💬 Contactar por WhatsApp
      </a>
    </div>

    <!-- Email directo -->
    <div style="margin-top:1rem;">
      <a href="mailto:marthagch85@gmail.com?subject=Consulta%20sobre%20bolsos%20de%20macram%C3%A9"
         class="btn-email">
        📧 Enviar correo a marthagch85@gmail.com
      </a>
    </div>
  </section>

  <footer>
    <div class="social-links">
      <a href="#">📷</a><a href="#">📘</a><a href="#">🐦</a><a href="#">📧</a>
    </div>
    <p>© 2025 Macramé Artesanal – Hecho con ❤️</p>
    <p style="margin-top:1rem;">📩 <a href="mailto:marthagch85@gmail.com" style="color:#d4a574;text-decoration:none;">marthagch85@gmail.com</a></p>
  </footer>

  <script>
    // Navbar effect
    window.addEventListener('scroll', () => {
      const navbar = document.getElementById('navbar');
      if (window.scrollY > 50) navbar.classList.add('scrolled');
      else navbar.classList.remove('scrolled');
    });

    // Contact form -> open mail client prefilled
    document.getElementById('contactForm').addEventListener('submit', function (e) {
      e.preventDefault();
      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      const message = document.getElementById('message').value.trim();
      const subject = 'Consulta desde el sitio web (macramé)';
      const body = `Nombre: ${name}\nCorreo: ${email}\nMensaje:\n${message}\n\n---\nEnviado desde bohemcr`;
      const mailto = `mailto:marthagch85@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
      window.location.href = mailto;
    });

    /*
      ✅ Robust image loader
      Tries multiple possible locations/filenames so your bags appear even if:
      - Files are at root next to index.html
      - Files are inside /img/
      - Extensions are .jpg or .JPG
      - (Optional) spaces/underscores handled by adding variants below
    */
    function tryLoadInto(imgEl, candidates) {
      let i = 0;
      const test = () => {
        if (i >= candidates.length) {
          // Final fallback placeholder to avoid empty box
          imgEl.src = "https://placehold.co/800x600/f8f5f2/2c3e50?text=Foto+no+encontrada";
          console.warn("No se encontró imagen para:", imgEl.id, candidates);
          return;
        }
        const src = candidates[i++];
        const probe = new Image();
        probe.onload = () => { imgEl.src = src; };
        probe.onerror = test;
        probe.src = src;
      };
      test();
    }

    // Add/adjust names here to match your real files exactly if needed:
    const beigeBaseNames = [
      "bolso-de-sobre",             // expected
      "Bolso-de-sobre",             // capitalized
      "bolso de sobre",             // with spaces (if your file had spaces)
      "bolso_de_sobre"              // underscores variant
    ];
    const azulBaseNames = [
      "aura-azul",
      "Aura-Azul",
      "aura azul",
      "aura_azul"
    ];

    function buildCandidates(basenames) {
      const exts = [".jpg", ".JPG", ".jpeg", ".JPEG", ".png", ".PNG"];
      const prefixes = ["", "img/"]; // root or /img folder
      const list = [];
      for (const b of basenames) {
        for (const p of prefixes) {
          for (const e of exts) list.push(`${p}${b}${e}`);
        }
      }
      return list;
    }

    // Try to load both images with many variations
    tryLoadInto(document.getElementById("img-bolso"), buildCandidates(beigeBaseNames));
    tryLoadInto(document.getElementById("img-aura"), buildCandidates(azulBaseNames));
  </script>
</body>
</html>
